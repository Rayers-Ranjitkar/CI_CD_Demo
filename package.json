{
  "name": "ci_cd",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "node --experimental-vm-modules node_modules/jest/bin/jest.js"
  },
  "_comment": "we are telling node that When someone runs npm test (or npm run test), execute the command jest. jest is a testing framework. When you run npm test: Node looks in package.json for the test script. + Changed type commonjs to module. This tells Node you’re using ES modules",
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
