{
  "name": "ci_cd",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "jest" 
  },
  "_comment" : "we are telling node that When someone runs npm test (or npm run test), execute the command jest. jest is a testing framework. When you run npm test: Node looks in package.json for the test script.",
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "jest": "^30.2.0"
  }
}
